<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>Contact Pro Manager</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
</head>

<body>

    <div class="container">
        <div class="row col card shadow mx-auto my-5">
            <div class="col-10 card shadow mx-auto mt-3">
                <div class="card-header text-center bg-dark text-light mt-2">
                    <h3>MODIFICATION D'UN CONTACT</h3>
                </div>
                <div class="card-content mt-3">
                    <div id="erreur" class="alert alert-danger mt-3">
                        erreurs
                    </div>
                    <form method="post" action="/contact/edit/<%= contact._id %>">
                        <div class="mb-3">
                            <label for="lastname" class="form-label">Nom</label>
                            <input type="text" name="lastname" id="lastname" value="<%= contact.lastname %>"
                                class="form-control" placeholder="Nom">
                        </div>

                        <div class="mb-3">
                            <label for="firstname" class="form-label">Prénom</label>
                            <input type="text" name="firstname" id="firstname" value="<%= contact.firstname %>"
                                class="form-control" placeholder="Prénom">
                        </div>

                        <div class="mb-3">
                            <label for="company" class="form-label">Société</label>
                            <input type="text" name="company" id="company" value="<%= contact.company %>"
                                class="form-control" placeholder="Société">
                        </div>

                        <div class="mb-3">
                            <label for="adress" class="form-label">Adresse</label>
                            <input type="text" name="adress" id="adress" value="<%= contact.adress %>"
                                class="form-control" placeholder="Adresse">
                        </div>

                        <div class="mb-3">
                            <label for="phone" class="form-label">Tel</label>
                            <input type="text" name="phone" id="phone" value="<%= contact.phone %>" class="form-control"
                                placeholder="Tel">
                        </div>

                        <div class="mb-3">
                            <label for="email" class="form-label">E-Mail</label>
                            <input type="email" name="email" id="email" value="<%= contact.email %>"
                                class="form-control" placeholder="E-Mail">
                        </div>

                        <div class="mb-3">
                            <label for="sector" class="form-label">Secteur</label>
                            <select name="sector" id="sector" class="form-select">
                                <option value="industrie" <%=contact.sector==='industrie' ? 'selected' : '' %>>Industrie
                                </option>
                                <option value="informatique" <%=contact.sector==='informatique' ? 'selected' : '' %>
                                    >Informatique</option>
                                <option value="sante" <%=contact.sector==='sante' ? 'selected' : '' %>>Santé</option>
                                <option value="education" <%=contact.sector==='education' ? 'selected' : '' %>>Education
                                </option>
                            </select>
                        </div>

                        <div class="mb-3 form-check">
                            <input type="checkbox" name="active" id="active" class="form-check-input" <%=contact.active
                                ? 'checked' : '' %>>
                            <label class="form-check-label" for="active">Actif ?</label>
                        </div>

                        <button type="submit" class="btn btn-dark">MODIFIER</button>
                    </form>

                </div>
            </div>
        </div>
</body>

</html>